import { AutosizeDropdownDirective } from './autosize-dropdown.directive';

describe('AutosizeDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new AutosizeDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
